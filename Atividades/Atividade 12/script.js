/* 

12° Atividade

Escreva uma função chamada ePaisagem que recebe dois argumentos, 
largura e altura de uma imagem (number). E Retorne true se a imagem estiver no modo
paisagem.

*/

const ePaisagem = (largura, altura) => largura > altura;

console.log(ePaisagem(1200, 1000));
